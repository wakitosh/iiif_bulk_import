<?php
$this->htmlElement('body')->appendAttribute('class', 'iiif-bulk-import');
echo $this->pageTitle('IIIF一括インポート');

$form = $this->vars('form');
?>

<div class="iiif-bulk-import-form">
	<div class="container">
		<?php
		echo $this->form()->openTag($form);
	echo $this->formRow($form->get('manifest_urls'));
		echo $this->formRow($form->get('csrf'));
		echo $this->formSubmit($form->get('submit'));
		echo $this->form()->closeTag();
		?>
	</div>
	<p class="explanation">IIIF Presentation v2/v3 のマニフェストURLを入力してジョブを開始します。</p>
</div>
